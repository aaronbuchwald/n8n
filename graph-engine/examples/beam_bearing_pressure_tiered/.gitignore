# A run output, not a source: `sources.write_json` writes it on every run, and a
# committed copy would be a second place the division of the population — and
# every governing force in it — is written down.
groups.json
__pycache__/
