# direnv: auto-load the nix dev shell when you cd into graph-engine/.
# First time only, run `direnv allow` to authorize this file.
# Requires nix (flakes enabled) + direnv with nix-direnv recommended for caching.
use flake
